# Demo data — do not delete

This directory contains a snapshot of the original demo / mock data used by the WiseOS MVP before switching to the real backend pipeline.

- `store.ts` – preserved copy of the hardcoded store defaults, course catalog (`KURSER`), and grading parameters.
- `submissions/` – sample handwritten PNG images and helper scripts used for end-to-end pipeline tests.
